The reason of separating app and yii2 framework in two directories:
+ To make app directory small, so that searching text becomes faster.
+ To make app directory small, so that copy or (git) exporting it easier.